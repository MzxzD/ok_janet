# macOS Client

macOS client for Janet Mesh Network.

## Implementation

The macOS client is similar to the iOS client but can also run as a service node.

## Building

```bash
cd clients/mac
xcodebuild -scheme JanetMeshClient -configuration Release
```

## Features

- Same features as iOS client
- Can also run as STT/TTS node
- Better performance for local processing
